# 📝 오늘 공부한 내용 복습 (NAVER 클론 코딩)

오늘은 네이버 메인 페이지를 클론 코딩하며 HTML/CSS의 핵심 기술들을 실습했습니다. 
아래 정리된 내용을 통해 오늘 사용한 주요 개념들을 복습해 보세요!

---

## 1. 문서의 뼈대 잡기: 시맨틱 태그(Semantic Tags)
HTML 문서를 작성할 때 의미를 가진 태그를 사용하여 문서의 구조를 명확하게 했습니다.
- `<header>`: 페이지의 상단 영역 (검색창, 네비게이션 아이콘 등)
- `<nav>`: 링크들의 모음 (메일, 카페, 블로그 등 바로가기 메뉴)
- `<main>`: 페이지의 주요 콘텐츠 영역
- `<section>`: 관련된 콘텐츠들을 묶어주는 영역 (뉴스스탠드)

## 2. 화면 배치의 핵심: Flexbox (`display: flex`)
요소들을 가로 또는 세로로 쉽게 배치하고 정렬할 수 있는 가장 많이 쓰이는 도구입니다. 검색창, 메뉴바, 로그인 위젯 등 거의 모든 영역에 사용되었습니다.
- **가로 배치**: `display: flex;`
- **가로 정렬**: `justify-content: space-between;` (양끝으로 밀어내기), `justify-content: center;` (가운데 정렬)
- **세로 정렬**: `align-items: center;` (요소들의 세로 중앙을 맞춤)
- **간격 띄우기**: `gap: 15px;` (요소들 사이에 일정한 여백 주기)

## 3. 격자형 배치: CSS Grid (`display: grid`)
뉴스스탠드의 언론사 로고들이 바둑판 모양으로 깔끔하게 배치된 영역에 사용되었습니다. 2차원적인 배치를 할 때 아주 유용합니다.
- `display: grid;`
- `grid-template-columns: repeat(6, 1fr);`: 가로로 6개의 칸을 동일한 비율(`1fr`)로 나누어 배치합니다.

## 4. 원하는 위치에 고정하기: Position 속성
화면을 스크롤해도 항상 같은 위치에 보이게 하거나, 특정 영역 안에서 자유롭게 위치를 잡을 때 사용합니다.
- **웨일 브라우저 팝업 (`position: fixed`)**: 화면(Viewport) 우측 하단에 항상 떠 있도록 고정했습니다.
- **팝업창 닫기 버튼 (`position: absolute`)**: 팝업창(부모)을 기준으로 우측 상단 모서리에 버튼을 정확히 배치할 때 사용했습니다.

## 5. 기초적인 동적 제어: JavaScript 이벤트 리스너
CSS의 디자인을 넘어, 사용자의 '클릭'에 반응하는 기능을 아주 간단한 JavaScript로 구현했습니다.
```javascript
// 닫기 버튼을 클릭했을 때
document.querySelector('.close-btn').addEventListener('click', function() {
    // 웨일 팝업창을 화면에서 숨긴다
    document.querySelector('.whale-popup').style.display = 'none'; 
});
```
- `.querySelector()`: 원하는 HTML 요소를 선택합니다.
- `.addEventListener('click')`: 해당 요소를 '클릭'하는지 감시합니다.

---

### 💡 오늘의 복습 포인트!
1. 완성된 브라우저 창에서 **개발자 도구(F12)**를 열어 각 요소들이 어떤 박스 형태를 띄고 있는지 확인해 보세요.
2. `style.css`에서 `display: flex;` 속성을 지우거나 주석(`/* */`) 처리해 보면서, Flexbox가 화면 배치에 얼마나 큰 영향을 주는지 눈으로 직접 확인해 보면 이해가 훨씬 빠릅니다.
